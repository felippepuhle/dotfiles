# /Users/felippepuhle/.config/tmuxinator/backend.yml

name: backend
root: ~/Projects/Patio/backend

windows:
  - editor:
      layout: main-vertical
      panes:
        - nvim .
  - hasura: cd hasura && hasura console
  - firebase-functions: cd firebase/functions && yarn emulate
  - api-server: cd api-server && yarn dev
